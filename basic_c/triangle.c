#include<stdio.h>
void main()
{
    int row=1, noofrow=4, col;
    
        while(row=noofrow)
        {
            col=1;
            while(col<= noofrow-(row*2-1)) &&
            (col>=noofrow+(row-1)){

            }
        }
    
}