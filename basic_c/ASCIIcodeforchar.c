#include<stdio.h>
void main(){
    // char ch;
    // printf("Enter any ch:");
    // scanf("%c",&ch);

    for (int i = 0; i <= 255; i++)
    {
        char num = i;
        printf("ASCII value of %d is %d \n",i, num);
        }
}
